pre-commit:
  parallel: true
  commands:
    fmt:
      glob: "*.go"
      run: gofmt -l -w {staged_files} && git add {staged_files}
    lint:
      glob: "*.go"
      run: golangci-lint run ./...

pre-push:
  commands:
    unit-tests:
      run: go test -short ./...
